/*#include <stdio.h>
int main()
{
    // We will not be using ctype.h here
    char ch;
    printf("Enter a character: \n");
    scanf("%c", &ch);
    // Check if the encoding lies between a and z
    if (ch >= 'a' && ch <= 'z'){
        printf("Lower case letter");
    }
    // Check if the encoding lies between A and Z
    else if (ch >= 'A' && ch <= 'Z'){
        printf("Upper case letter");
    }
    // Check if the encoding lies between 0 and 9
    else if (ch >= '0' && ch <= '9'){
        printf("Digit");
    }
    // Return error
    else{
        printf("Error");
    } */
    /*
    // Another style of answer
    switch(ch){
    case 'a' ... 'z':
        printf("Lower case letter");
        break;
    case 'A' ... 'Z':
        printf("Upper case letter");
        break;
    case '0' ... '9':
        printf("Digit");
        break;
    default:
        printf("Error!");
        break;
    }
    */ /*
    return 0;
}
*/

